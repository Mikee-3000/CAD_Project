<%= form_with(model: treaty) do |form| %>
  <% if treaty.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(treaty.errors.count, "error") %> prohibited this treaty from being saved:</h2>

      <ul>
        <% treaty.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :date_of_signing, style: "display: block" %>
    <%= form.date_field :date_of_signing %>
  </div>

  <div>
    <%= form.label :place_of_signing, style: "display: block" %>
    <%= form.text_field :place_of_signing %>
  </div>

  <div>
    <%= form.label :active, style: "display: block" %>
    <%= form.check_box :active %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
